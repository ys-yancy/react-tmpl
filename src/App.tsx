/** @format */

import React from 'react';
import ReactDOM from 'react-dom';

const App: React.FC = () => {
    return <div>weekdone</div>;
};

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root'),
);
